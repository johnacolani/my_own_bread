import 'package:flutter/material.dart';

/// Warm bakery palette — cream surfaces, sage primary, peachy CTAs.
class AppColors {
  /// Sage with a slightly warmer, softer feel than flat teal.
  static const Color primary = Color(0xFF7BA698);
  static const Color primaryPressed = Color(0xFF679486);
  /// Apricot–coral for actions (less “corporate” than flat coral).
  static const Color coral = Color(0xFFE5987A);
  static const Color coralPressed = Color(0xFFD88768);
  static const Color purple = Color(0xFF8B7FA8);
  static const Color gold = Color(0xFFD4B87A);
  /// Warm cream page background.
  static const Color background = Color(0xFFF8F3EA);
  /// Soft paper white with a hint of warmth.
  static const Color surface = Color(0xFFFFFAF4);
  static const Color surfaceMuted = Color(0xFFFFF2E6);
  /// Warm charcoal (not harsh black).
  static const Color textPrimary = Color(0xFF2C2825);
  static const Color textSecondary = Color(0xFF6E6860);
  /// Visible but soft warm edge.
  static const Color border = Color(0x33C4B5A8);
  static const Color success = Color(0xFF5C8F6E);
}
